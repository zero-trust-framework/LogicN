# LogicN DB PostgreSQL

`logicn-db-postgres` is a future PostgreSQL adapter contract package.

It must consume `logicn-data-db`, `logicn-data-model`, `logicn-data-query` and
`logicn-data-response` contracts. It must not bypass typed models, validation,
parameterisation, permissions, safe response mapping or reports.
