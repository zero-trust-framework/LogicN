{
  "name": "@logicn/tools-benchmark",
  "version": "0.1.0-beta.0",
  "description": "LogicN benchmark and diagnostics contracts for logic, compute targets, fallback behaviour and safe reporting.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "npm run typecheck"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
