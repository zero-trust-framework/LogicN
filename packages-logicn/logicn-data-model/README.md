# LogicN Data Model

`logicn-data-model` defines typed storage model contracts.

Use this package for:

```text
table and collection mapping
field type contracts
primary and unique key metadata
personal data field classification
secret field classification
hidden/internal field policy
model-level permission metadata
archive and retention references
```

Database models are storage models. They are not automatically safe API
responses.
