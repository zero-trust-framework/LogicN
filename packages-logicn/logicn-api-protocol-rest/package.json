{
  "name": "@logicn/api-protocol-rest",
  "version": "0.1.0",
  "description": "Reference REST protocol-adapter (L3): /src governed flow → fusable .wasm, fused via the App Kernel. Proves the /src → .wasm → fused path end-to-end.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../../logicn.mjs build --package .",
    "test": "node ../logicn-core-compiler/node_modules/typescript/bin/tsc -p ../logicn-framework-app-kernel/tsconfig.json && node ../../logicn.mjs build --package . && node --test tests/*.test.mjs"
  }
}
