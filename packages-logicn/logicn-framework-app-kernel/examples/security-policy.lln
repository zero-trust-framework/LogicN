security {
  api_methods {
    allow [GET, POST]
    deny [PUT, PATCH, DELETE, CONNECT, TRACE]
  }

  inbound_ports {
    allow [80, 443]
    deny_all_other true
  }

  file_access {
    default "deny"
  }

  shell {
    default "deny"
  }
}
