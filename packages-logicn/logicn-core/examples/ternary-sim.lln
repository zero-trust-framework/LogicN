// EXPECT: ACCEPT
// AREA: type-system
// PURPOSE: Tri enum with pure flow and guard-clause match
enum Tri {
  Positive
  Neutral
  Negative
}

pure flow signalState(score: Float) -> Tri {
  match score {
    score > 0.1  => Positive
    score < -0.1 => Negative
    _ => Neutral
  }
}
