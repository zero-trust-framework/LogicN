// EXPECT: ACCEPT
// AREA: basic
// PURPOSE: type aliases, record declaration, and typed field access
type CustomerId = String

type Customer {
  id: CustomerId
  name: String
  email: Option<String>
}

flow displayName(customer: Customer) -> String {
  return customer.name
}
