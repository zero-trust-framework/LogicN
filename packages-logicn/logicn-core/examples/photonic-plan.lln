pure flow opticalSignalScore(input: Vector<8, Float32>) -> Vector<8, Float32> {
  compute target photonic {
    fallback gpu
    fallback cpu

    output = signalTransform(input)
  }

  return output
}
