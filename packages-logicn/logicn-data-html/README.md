# LogicN Data HTML

`logicn-data-html` defines HTML parsing, sanitization, rendering and extraction
contracts.

Use this package for:

```text
HTML parse contracts
safe HTML render contracts
sanitization policy
link and text extraction
metadata extraction
HTML search document creation
unsafe element and attribute reports
```

It must not become a browser engine or layout engine.
