# LogicN DB Firestore

`logicn-db-firestore` is a future Firestore adapter contract package.

It must consume typed model, query and response contracts. Document data must
respect validation, permissions, safe response mapping, archive and report
policies.
