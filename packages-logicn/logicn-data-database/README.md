# LogicN Data Database

`logicn-data-database` defines database archive/export contracts.

Use this package for:

```text
database export contracts
snapshot metadata
schema version metadata
row count and checksum reports
restore validation references
redaction and classification hooks
```

It must not become an ORM, migration tool or database engine.
