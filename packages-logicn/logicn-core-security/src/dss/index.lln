// DSS — Deterministic Security Supervisor
// DRCM Phase 5 Foundation Files
//
// Build order (spec: logicn-governed-tower-specification.md §5):
//   1. vdpm.lln            ← V_DPM bit layout + capability mapping + O(1) check  [THIS PHASE]
//   2. capability-map.lln  ← SystemCapabilityType enum + string→bit converter     [#38]
//   3. emergency-sm.lln    ← Emergency state machine transitions                  [#39]
//   4. trap-handler.lln    ← unreachable intercept + AuditEvent emission          [#76]
//   5. dss-supervisor.lln  ← Main DPM tracking loop + WASI import broker          [#41]
//
// The entire DSS is a governed .lln program compiled to WASM.
// It runs as a Wasmtime component alongside guest DWI isolates.
// It CANNOT be replaced with Rust — see architecture-charter.md.

// use security::dss::vdpm   (import keyword not yet in Stage A — #38 prerequisite)
