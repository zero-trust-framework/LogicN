{
  "name": "@logicn/tri-pipe",
  "version": "1.0.0-beta.2",
  "description": "The Tri-Pipe capstone: createTriPipeEngine() composes the hardware() capability directive + the photonic backend/router + the governed HybridInferenceEngine into one call, selecting the digital registry and photonic offload by the resolved {binary|hybrid|photonic} tier. Digital is the default; photonic only on a proven net win; fail-closed to binary.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "prove": "node scripts/prove-tri-pipe.mjs"
  },
  "dependencies": {
    "@logicn/inference-bridge-contract": "file:../logicn-inference-bridge-contract",
    "@logicn/hardware-tier": "file:../logicn-hardware-tier",
    "@logicn/ext-photonic-emulator": "file:../logicn-ext-photonic-emulator",
    "@logicn/tower-citizen": "file:../logicn-tower-citizen"
  },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.5.0" },
  "keywords": ["logicn", "tri-pipe", "photonic", "capstone", "composition", "hardware", "fail-closed"],
  "note": "The composition/application layer — the one package allowed to depend on the Tower runtime. Ties together the 2026-06-20 loop session's photonic line (0053 emulator + 0054 directive/loader + the Tower dispatch wiring). Deps imported by relative dist path (repo convention; offline)."
}
