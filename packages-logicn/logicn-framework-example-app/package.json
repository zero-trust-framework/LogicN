{
  "name": "@logicn/framework-example-app",
  "version": "0.1.0",
  "description": "The canonical runnable 'hello, governed world' LogicN app: a governed flow compiled to a signed .wasm, fused into the App Kernel at a route, and served over HTTP. This is the golden template `logicn new app` emits.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "build:greeting": "node ../../logicn.mjs build --package packages/greeting --no-refresh",
    "test": "npm run typecheck && npm run build && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@logicn/framework-app-kernel": "file:../logicn-framework-app-kernel",
    "@logicn/framework-api-server": "file:../logicn-framework-api-server"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
