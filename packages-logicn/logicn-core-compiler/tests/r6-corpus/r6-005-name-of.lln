// EXPECT: ACCEPT
// R6 corpus 005 — R5: match + Option
pure flow nameOf(code: Int) -> Option<String>
contract { intent { "Map a code to an optional name." } }
{
  match code {
    1 => { return Some("alpha") }
    2 => { return Some("beta") }
    _ => { return None }
  }
}
