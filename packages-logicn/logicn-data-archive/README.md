# LogicN Data Archive

`logicn-data-archive` defines archive and integrity contracts.

Use this package for:

```text
archive item contracts
content-addressed references
manifest contracts
hash and checksum metadata
signature metadata
retention policy references
integrity verification reports
restore reports
```

It should not implement object storage, backup systems or retention engines.
