{
  "name": "@logicn/framework-api-server",
  "version": "0.1.0-beta.0",
  "description": "LogicN HTTP API-server adapter: a thin node:http transport that buffers the request body under a hard DoS cap and hands every request to the non-bypassable App Kernel. It never pre-empts a kernel gate except the additive body cap.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "npm run typecheck && npm run build && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@logicn/framework-app-kernel": "file:../logicn-framework-app-kernel"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
