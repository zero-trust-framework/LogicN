# LogicN DB OpenSearch

`logicn-db-opensearch` is a future OpenSearch adapter contract package.

It must consume typed model, query and response contracts. Search-index data
must also respect `logicn-data-search` and privacy/report policies.
