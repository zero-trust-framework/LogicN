{
  "name": "@galerinaa/core-economics",
  "version": "1.0.0-beta.2",
  "description": "CostGraph, ValueGraph, and risk-adjusted execution routing for the Galerina platform. Economics is a constraint layer that sits below governance — it can pull the emergency brake on a safe path, but never press the gas pedal on an unsafe one.",
  "license": "Apache-2.0",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "private": true,
  "note": "Economics sits BELOW governance. It reads ProofGraph; it never modifies it."
}
