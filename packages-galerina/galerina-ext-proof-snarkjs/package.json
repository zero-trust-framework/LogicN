{
  "name": "@galerina/ext-proof-snarkjs",
  "version": "1.0.0-beta.2",
  "description": "snarkjs Groth16 prover backend for Galerina epilogue { generate_proof zk_snark_receipt }. Phase 1: pure-JS Groth16 circuit over sha256(sourceText + contractHash). Non-core extension — the compiler core never imports this directly.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "snarkjs": "^0.7.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.5.0"
  },
  "private": true,
  "keywords": ["galerina", "zk-snark", "groth16", "snarkjs", "proof", "epilogue"]
}
