---
globs: '["**/packages-lo/**/*"]'
description: All new packages under the 'packages-lo' directory should follow
  the naming convention of 'lo-' followed by a category and name, e.g.
  'lo-logic-compute'. Do not shorten package names (e.g. lo-fw instead of
  lo-framework).
alwaysApply: true
---

Use the package naming convention of 'lo-' followed by a category and name for all new packages under the 'packages-lo' directory