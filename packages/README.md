# Packages

`packages/` is reserved for normal app/vendor package space.

LogicN language, runtime, target, tooling and domain packages now live in
`packages-logicn/`.

Do not add LogicN package contracts here. Use this directory only when the host app
needs ordinary ecosystem packages that are not part of the LogicN package
collection.
