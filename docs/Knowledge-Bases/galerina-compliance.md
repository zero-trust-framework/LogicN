# Galerina — Compliance as Architecture

**Version: 1.0 — 2026-06-01**
**Status: Canonical positioning**

---

## The Core Shift

Traditional compliance asks:

```
Did we follow the rules?
Can we prove it?
What happened?
Who authorised it?
```

These are answered **after the fact** — by parsing logs, interviewing engineers, reconstructing event sequences, and hoping the audit trail is complete.

Galerina answers them **before execution**:

```
What is allowed?                  → GovernanceGraph
Why is it allowed?                → ProofGraph
Who authorised it?                → CapabilityGraph
What data did it touch?           → PrivacyGraph + LineageGraph
What did it cost?                 → CostGraph
What happened?                    → AuditGraph
```

The compliance record is not assembled from logs.
It is **generated by the execution architecture**.

---

## The Simple Statement

```
Compliance is proven by architecture.
```

Not by policy documents. Not by audit scripts. Not by retrospective log analysis. By the structure of how the system executes.

---

## How It Works

### What Governance Provides

```
GovernanceGraph  → what is allowed, by whom, under what conditions
ProofGraph       → proof of legality before execution
CapabilityGraph  → who holds which capabilities
PrivacyGraph     → what protected data exists and how it must be handled
LineageGraph     → where data came from and where it went
AuditGraph       → immutable record of every governed execution
CostGraph        → what every execution cost
```

### The Execution Flow

```
[ Traditional Software ]
Deploy code → run → breach/error → investigate → reconstruct → fine

[ Galerina ]
Compile → ProofGraph rejects illegal flows → governance proven before execution
```

If a developer writes code that routes unredacted patient data to an unauthorised external model, **the compilation fails**. The breach never happens. There is no incident to investigate.

### The Audit Record (Phase 26B)

Every governed execution produces a machine-readable audit record:

```json
{
  "schemaVersion": "fungi.audit.v1",
  "audit_sequence": 14092,
  "flow_intent": "process_patient_billing",
  "governance_id": "gov_flow_89f2a7",
  "proof_graph_hash": "sha256:4d7e...",
  "hardware_domain": "arm.sve2",
  "hardware_governance_class": 1,
  "proof_escalation_level": 2,
  "input_seal": "sha256:e3b0...",
  "output_seal": "sha256:8ca6...",
  "lineage_retention": "7_years",
  "capability_used": "medical.read",
  "pii_redacted": true,
  "audit_written": true
}
```

This is not compiled from logs. It is **generated natively by the ExecutionGraph Kernel** as instructions are scheduled.

---

## Regulatory Compliance Mapping

### EU AI Act — Article 12 (Event Logging for High-Risk Systems)

**Requirement**: High-risk AI systems must maintain untampered, automated event logging across their lifecycle.

**Galerina response**:
- `AuditGraph` is append-only and immutable — cannot be altered post-execution
- `InputSeal` + `OutputSeal` for any NPU/TPU/ANE target — mathematically proves what entered the AI model and what came out
- `ProofGraph` records which policies were applied before execution
- `galerina-verify proof.json` CLI: any auditor can offline-verify the proof chain

**Gap closed**: EU AI Act requires logging for *black-box* models. Galerina's Input/Output Seal means the internal state of an NPU or TPU never needs to be inspected — the seals prove the transformation boundary cryptographically.

---

### EU AI Act — Article 13 (Transparency for Users)

**Requirement**: High-risk AI systems must be transparent about their capabilities and limitations.

**Galerina response**:
- `contract.ai { approved_models { ... } }` — the permitted models are declared in source code, not a configuration file
- `AuditGraph` records which model was invoked and with what inputs
- `GovernanceSignature` (Phase 39) provides unforgeable evidence the ProofGraph was issued by a specific compiler version

---

### HIPAA — Security Rule (§164.312)

**Requirement**: Access controls, audit controls, integrity, and transmission security for PHI.

**Galerina response**:
- `contract.privacy { pii { ... } require protected_boundary before ai.infer }` — enforced at compile time
- `PrivacyGraph` tracks PII through every flow
- `LineageGraph.retention 7_years` — automatic data retention compliance
- Audit controls: `AuditGraph` records every PHI access with actor, trace_id, timestamp
- Integrity: `InputSeal`/`OutputSeal` prove data was not modified in transit to accelerators
- `require local_execution` in AI contracts — PHI never leaves the device

---

### SOC 2 — Trust Service Criteria

**Requirement**: Security, availability, processing integrity, confidentiality, privacy.

| Criterion | Galerina Component |
|---|---|
| Security (CC6) | `CapabilityGraph` — no access without declared capability |
| Availability (A1) | `contract.limits { request_time 100ms }` — SLA declared in source |
| Processing Integrity (PI1) | `ProofGraph` — every execution is pre-verified |
| Confidentiality (C1) | `PrivacyGraph` + `require redaction before audit.write` |
| Privacy (P1-P8) | `LineageGraph` + `contract.privacy` — full GDPR/CCPA/HIPAA mapping |

---

### SEC Rule 17a-4 — Records Retention and Authenticity

**Requirement**: Financial records must be unalterable, authentic, and reproducible.

**Galerina response**:
- `AuditGraph` is append-only — records cannot be deleted or altered
- `GovernanceSignature` (Phase 39) provides unforgeable proof of who generated each compliance certificate
- `InputSeal`/`OutputSeal` on every financial model execution — auditors can verify any transformation
- `LineageGraph` — full data lineage for every financial calculation

---

### ISO 27001 — Information Security Management

**Requirement**: Systematic management of information security through controls.

**Galerina response**:
- All controls declared in source — `contract.effects`, `contract.privacy`, `contract.ai`
- Controls enforced at compile time — not configurable at runtime by operators
- `AuditGraph` provides continuous evidence of control effectiveness
- `CapabilityGraph` implements ISO 27001 Annex A.9 (Access Control) structurally

---

### NIST CSF 2.0

| Function | Galerina Component |
|---|---|
| Identify | `GovernanceGraph` maps all assets, capabilities, effects |
| Protect | `CapabilityGraph`, `PrivacyGraph`, compile-time effect enforcement |
| Detect | `AuditGraph` + (future) neuromorphic always-on monitoring |
| Respond | `ProofGraph` enables instant root-cause analysis |
| Recover | `LineageGraph` enables full data reconstruction |
| Govern | The entire Galerina stack is the governance function |

---

## The `galerina-verify` CLI (Phase 34+)

```bash
galerina-verify proof.json --key fungi-gov-2026-01
```

Output:
```
✓ Governance: verified (sha256:4d7e...)
✓ Input seal: sha256:e3b0...
✓ Output seal: sha256:8ca6...
✓ Capability: medical.read declared and held
✓ Privacy: PII redacted before audit.write
✓ Hardware: arm.sve2 (ExecutionPlane, ProofLevel.Sealed)
✓ GovernanceSignature: fungi-gov-2026-01 (valid, ML-DSA)

RESULT: COMPLIANT
```

Any external auditor, regulator, or compliance officer can run this against any Galerina proof file — no access to source code, infrastructure, or cloud accounts required. The proof is self-contained.

---

## What This Replaces

| Traditional | Galerina |
|---|---|
| Weekly log rotation and audit script | Continuous AuditGraph, always current |
| Weeks of log analysis before audit | `galerina-verify` runs in seconds |
| "We believe we followed policy" | "Here is the cryptographic proof" |
| Black-box AI explanation | InputSeal + OutputSeal prove the transformation |
| Manual lineage reconstruction | LineageGraph generated at compile time |
| Retrospective compliance | Prospective compliance (blocked at compile time) |

---

## The Commercial Case

In regulated industries, governance is **shifted cost**, not **added cost**.

Traditional:
```
Engineering cost (compliance)   → ongoing (manual log checking, audit prep)
Compliance incident cost        → high (breach investigation, fines, remediation)
Total                           → unpredictable, high
```

Galerina:
```
Engineering cost (governance)   → zero at runtime (compile-time verification)
Compliance evidence             → continuous, automatic, always current
Total                           → predictable, low
```

The estimated audit savings from the ROI report:
```
2.5 hours × £80/hr × 1 proven flow = £200 saved per compliance inquiry
```

At scale, a governed Galerina service with 10,000 governed flows saves £2,000,000 per major audit cycle — not counting breach prevention.

---

## See Also

- `galerina-master-architecture.md` — The four-way separation
- `galerina-governance-signature.md` — GovernanceSignature (quantum-resistant evidence)
- `galerina-core-economics-package.md` — CostGraph total cost formula
- `galerina-governance-economics-platform.md` — Risk-adjusted routing and ROI
- `galerina-hardware-compute-fabric.md` — Input/Output seals for hardware dispatch
