# Absorbed R&D — TLSTP/transport-auth CLOSURE cluster (0071–0077)

The worker's completed closure + verification tasks for the 0065–0070 transport/auth cluster, absorbed into the KB.
Canonical full reports: `C:\wwwprojects\LogicN-R-AND-D\_session-bridge\done\007{1..7}-*.done.md`. All are
DESIGN/SPEC/VERIFICATION (no production code). Bridge tasks 0001–0077 are now all done. The R&D side is **fully
closed**. Binding posture throughout: crypto stays Binary; fail-closed; prove-own-maths (artifacts re-run to exit 0).

## 0071 — Governed Trust Capsule signing-spec reconciled ✅ (production fix APPLIED this session)
The §2-vs-§8 contradiction is resolved to **ONE method: `M = CBOR(Sig_structure)`, signed DIRECTLY, ML-DSA `ctx=""`
(empty), NO SHA-256 pre-hash (RFC 9964 / RFC 9052 §4.4).** §8 step 4 was the defect. Rationale (recorded in §8):
(a) standards — RFC 9964 signs `Sig_structure` directly (pre-hash is non-conformant; "pure" ML-DSA ≠ HashML-DSA);
(b) a SHA-256 pre-hash caps the signed message at ~2¹²⁸, clamping the hybrid below ML-DSA-65's NIST Cat-3 margin;
(c) no streaming gain. **The 3-edit production fix the worker handed back (production spec is read-only for it) was
APPLIED to `packages-logicn/logicn-ext-tmf/spec/governed-trust-capsule-v0.md`** (§8 steps 4/5 + the canonical-method
blockquote) — grep confirms no residual bare-defect definition. Unblocks the capsule reader (#12).

## 0072 — Prove-maths closure ✅ (7 verdicts → PROVEN)
Discharges the prove-maths debt the 0067 re-audit surfaced. Verdict deltas (each with a re-runnable
independent-ground-truth artifact in `tri-encription/bench/`, exit 0):
- **0014-C3** i32 overflow-equivalence SAMPLED → **PROVEN** (Z3 delegate, negation-UNSAT over 2⁶⁴); 0014-C1 aggregate re-evaluated → PROVEN.
- **0024-C1/C3** div/rem trap-set `X==X` tautology → **PROVEN** real cross-encoding (spec trap DERIVED from `bvsdiv`/`bvsrem` definedness; `Xor(spec,native)` UNSAT; non-vacuity teeth-tests → SAT witness INT32_MIN,-1).
- **0031-C1** No-Coercion, **0031-C3** param-taint-on-unknown-origin, **0031-C7** fuse-loader gates, **0022-A4** deadline, **0022-B5** zeroizeAfterUse → **PROVEN** vs the SHIPPED dist binary.
- **0059** L1 authority-monotonicity / L3 dispatch-totality / n-ary fold → **PROVEN** (18 SMT obligations).
- **Stays open (honest):** 0024-C2/C4 div/rem VALUE identities **EXCL-SLV** (Z3 BV-division solver limit); **X1** live 3-tier dispatch **EXCLUDED** (production read-only, no instrumented harness); 0022-B4 JIT residue OPEN.

## 0073 — R&D-record hygiene ✅
4 stale-claim corrections applied (as dated callouts) to the R&D done-records: **0031-C5** (34A IS built; only 34B
remains), **0014-C6** (integer fork RESOLVED Fork-A=TRAP), **0014-C5** (walker now TRAPS), **0022-A4** (deadline now
fail-closed); + citation re-anchors (0022-A3, nmr-C4 mis-pointer → `nmr-verify.mjs`, 0024 `.smt2` run-path). Two new
canonical 0054 proof scripts authored + run green: `rd-hardware-capability-directive-proof.mjs` (24/24), `rd-tier-package-orthogonality-proof.mjs` (23/23). NO verdict changes.

## 0074 — Closure index ✅
Confirms all Section-A closure tasks (0071–0073) + the follow-on (0075–0077) done + verified; Section-B hub builds +
Section-C accepted-as-open acknowledged, not re-queued. (See its §B/§C, mirrored below.)

## 0075 — 3 defensive-publication notes WRITTEN ✅ (correcting "not yet written")
Written into **`C:\wwwprojects\LogicN-Patens\`** (NOT a paper — timestamped prior-art records, novelty explicitly
disclaimed): `note-01-no-coercion-degrade-only-k3-composition.md`, `note-02-prove-own-maths-methodology-and-measured-negatives.md`,
`note-03-crypto-on-core-rejected-path-record.md`. Each cites the prior art (Zdancewic-Myers/Bruns-Huth/Birgisson;
Pnueli-Necula/GRADE-SLSA/Livshits/IBM-Haifa/Woodcock; NIST SP 800-90 + Rührmair) + disclaims novelty. All landed in
`LogicN-Patens` (the ScientificPapers README pre-grades these as repo-prior-art, not papers). **NB: `LogicN-Patens`
is NOT a git repo — the notes are on disk, un-versioned; `git init` is an owner decision (not done).**

## 0076 — Measured-negative benchmark ✅ (verdict: borderline-NO even reframed)
Re-runnable bench (27/27, exit 0) modelling K3 hard-fail vs soft-fail across a **cited** OCSP/CRL outage-rate range;
closed-form false-deny-vs-MITM-exposure trade validated by seeded Monte-Carlo; LogicN-vs-baseline column included.
**Honest verdict: a *qualified* measured-negative — borderline-NO as a standalone paper** (largely re-derives the
known Web-PKI soft-fail consensus); publishable as a short workshop note ONLY if reframed around the K3 framing + a
first-party availability measurement. So the one conditional paper path from this corpus is **still essentially no.**

## 0077 — Kleene-lattice unify (#205) ✅ → ADOPT-WITH-CHANGES
Share the meet-semilattice + fail-closed/deny-by-default story across the type-confidence and governance axes, but
**do NOT collapse the two axes into one indistinguishable value** — keep per-axis provenance + a per-axis boundary
rule. No-Coercion (`e=vAnd(t*,r)≤t*`) carries with a seedless-fold change. **Category-conflation is real:** an
ill-typed program is a STRUCTURAL reject, not a policy DENY. Honest correction: the type + value-state axes are NOT
trit-shaped today (sentinel + bitset/diagnostic), so the third (INDETERMINATE) element must be *added*, not assumed.

## Hub builds (Section B — tracked in the ledger/roadmap, NOT R&D-session work)
S1 K3 cert-gate (build-first; also the `kernel.ts:307` presence-auth fix) · the B8 adapter set · **34B routeDecl
auto-taint** (closes the one boundary fail-open; 0073 confirms 34A ships) · 0050 exporter + 0069 DTM fold · 0070
TamperTrust core + `cnf` binding · least-privilege declared-vs-used + `gasCharge()` · 0064 package-graph ·
fault-handler/hybrid-tier + the 0016 infra. Owner build order in progress: **BLD-003 ✅ → #211 → S1.**

## Accepted-as-open (Section C — genuine limits, not chased)
0024-C2/C4 div/rem VALUE identities (Z3 BV-division solver limit) · 0068 app-level response provenance over a
standard chain (needs peer cooperation) · no CA-abolition / no photonic-auth / no "unspoofable" optical-PUF
(PAC-learnable) · substrate/HW-gated isolation (DRCM/DSS.wasm, #102-106, cold-boot zero-wipe).

*Canonical reports: `…/_session-bridge/done/007{1..7}-*.done.md`. Defensive-pub notes: `LogicN-Patens/note-0{1,2,3}-*.md`
(un-versioned). Related: [[logicn-tlstp-transport-auth-rnd]], [[logicn-ip-paper-strategy]].*
