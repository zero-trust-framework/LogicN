# R&D 0059–0064 triage + actionable ledger (2026-06-22)

Hub triage of the six R&D worker done-reports `_session-bridge/done/0059..0064` (R&D repo
`C:\wwwprojects\Galerina-R-AND-D`, all pushed origin/main). Every load-bearing claim below was
**ground-truthed against live production source this session** (3-agent verify workflow `wgf61dnbx`,
346k subagent tokens) — the workflow corrected three of 0063's own framings (noted inline). Design only;
no production code changed by this triage. The convention follows
[galerina-rd-corpus-closure-2026-06-18.md](galerina-rd-corpus-closure-2026-06-18.md) (which covers 0036–0052;
0053–0058 are productionised in [galerina-build-roadmap.md](galerina-build-roadmap.md)).

## 1. The six at a glance

| # | Topic | Verdict | Net-new build? |
|---|---|---|---|
| **0059** | Formal proof structure for the fault-tolerance model + external-review triage | Global Safety Theorem `observable(e) ⇒ Binary(e) ∨ Fallback(Binary(e))`; Tri-Pipe = a **chain** (not a lattice); Byzantine handled **via DENY** not 3f+1 (category mismatch confirmed); proved/tested/assumed table; Freivalds/NMR assumptions stated | **Mostly already absorbed** into [galerina-fault-tolerance-and-stability.md](galerina-fault-tolerance-and-stability.md). Net-new = the SMT obligations (→ 0024/0040 Z3 track) + the idempotency effect-annotation |
| **0060** | TypeScript 7 "Go rewrite" + parallelization for toolchain speed | **BORROW** host `tsc` (TS7 native-preview, gate on stability) + package-level parallelism + watch; **REJECT** intra-pass parallelism (breaks determinism / #143) and a compiler rewrite (conflicts with Axis-B self-hosting). Measured baseline EXCLUDED (deps not installed in checkout) | Small DX/CI item; owner-go'd; needs a real baseline bench first |
| **0061** | First dedicated compiler R&D | Pipeline confirmed file-by-file. **Headline:** `TypeId.Unknown` already *parks* — the gap is it is a parking **sentinel**, not unified with the governance K3 **algebra**. Golden move = **one Kleene lattice across type-confidence + governance-verdict**. Keep TS Stage-A as the diagnostic oracle, finish Axis-B (5/8 passes already `.spore`); Z3 stays the FV workhorse | **Yes** — the Kleene-lattice unification + per-block differential emit-validation |
| **0062** | 3rd-party package architecture (zero-trust, golden-standard) | Fixed **package-standard profile** (NOT a new `permissions{}` block — one capability surface = `effects{}`, no drift); **transitive `effects(child) ⊆ mask(parent)` compile-time proof** (beats depth-limit-1, which is unworkable); `@galerinaa-core/*` verified tier + level-1-only deep deps; full **contract digest** | **Yes** — 4 design items |
| **0063** | AI-driven chain-of-attack prevention | Taxonomy (deep-dep / clean-but-malicious / governance-crafting / prompt-injection) → cited shipped control + honest residual; the **defense-asymmetry** principle (the attacker's AI cannot *confer* capability; every artifact meets the same fixed O(1) gates; soft spot = the grant/sign boundary). **Least-privilege minimality (declared-vs-used) is the unwired gap** | **Yes** — the convergent #1 build (shared with 0062) |
| **0064** | Should 3rd-party packages have a graph plugin? | YES — but as a **trusted built-in `galerina graph --package <dir>` + a central signed-package auditor** over the *signed admission surface*, NOT a 3rd-party-installable plugin (a self-declared graph is a trust hazard; must be derived-from-verified, never bypass the fuse border) | **Yes** — net-new tooling |

**Convergent thread (0062 ∧ 0063 ∧ 0064):** all three orbit one capability-containment core. The single
highest-value build is the **least-privilege minimality check + the transitive mask-⊆ proof**; the
signed-package audit graph (0064) is the legibility layer that makes both *visible before fusing* and
operationalises the 0063 grant/sign soft spot.

## 2. How 0063 affects Galerina packages (grounded map)

0063 is **design-only and cites already-shipped controls** — its "impact" is a map of which packages carry
each defense, **plus the one new gap**. Verified file:line this session (corrections to 0063's framing flagged):

| 0063 control | Owning package | Status | Evidence (verified) |
|---|---|---|---|
| 3-gate fuse border (hash · Ed25519+revoke · closed caps) | **`@galerinaa/framework-app-kernel`** `fuse-loader.ts` | shipped | Gate1 hash L483-489, Gate2 sig L308-361, Gate2b revoke L516-526 (fail-closed), Gate3 closed caps `buildCapabilityImports` L419-439. **⚠ correction:** this file throws `SPORE-FUSE-*`, it does **not** raise the `LinkError→CRITICAL` 0063 attributes to it |
| `LinkError → CRITICAL_SECURITY_VIOLATION` admission | **`@galerinaa/core-compiler`** `wasm-runtime.ts` | shipped | `admitAndInstantiate` L340-367 (attestation-first L351; disallowed-import→CRITICAL L362-366); `verifyWasm` L101-130. 0063 conflated this with the fuse-loader — they are **two complementary mechanisms in two packages** |
| Fail-closed revocation registry | **`governance/`** `revocation-registry.mjs` | shipped | `isKeyRevoked` L69-71; `assertRegistryTrustworthy` L142-176 (v2 trust-anchor pin). v3 rotation/threshold = documented TODO |
| `SPORE-MONO-001` capability-expansion block | **`@galerinaa/core-compiler`** `governance-verifier.ts` | shipped (**scoped**) | `verifyPolicyMonotonicity` L2883-2950 — **confirmed scoped to `emergency{}` transitions only** (`continue`s if no emergency child). So authority *expansion* is blocked but only on emergency transitions — see gap #2 |
| `ai{}` budget gates (`ERR_AI_CALL_BUDGET` / `_TOKEN_BUDGET`) | **`@galerinaa/tower-citizen`** `hybrid-engine.ts` | shipped | `checkAiGovernance` L523-550 |
| per-sink network egress / taint fence | **`@galerinaa/core-compiler`** `value-state-checker.ts` | shipped | `SPORE-SECRET-002` raw-secret→egress L1427; `SPORE-PRIVACY-002` cleartext-embedding→sink L1497-1504; `seal()`/`redact()` sole declassifier. **⚠ correction: 0063 mislabelled this as `@galerinaa/core-sentinel-egress` (LSEG)** — LSEG is the *audit-ledger* HMAC-chained write path, **not** a network egress fence |
| behavioral/CFG fingerprint | **`@galerinaa/core-compiler`** `manifest-generator.ts` | shipped (**effects-only**) | `behavioralFingerprint` L611-616 = sha256 of `name:qualifier:[effects]` — **confirmed effects-only** (the "contract digest" gap, #3). Note: the separate `sourceHash` *does* bind limits/substrate/invariant under the signature — just not in the dedicated runtime-deviation oracle |
| least-privilege minimality (declared-vs-used) | **`@galerinaa/core-compiler`** `effect-checker.ts` | **partial** | **⚠ 0063's "unwired" is imprecise.** `SPORE-EFFECT-002 OVERDECLARED_EFFECT` L521-532 **already detects** declare-without-use — but at **`severity:'warning'`** (non-blocking) and it is **omitted from the Stage-B self-hosted `effect-checker.spore`** (which emits only 001/003/004/005). Net-new delta = escalate to a profile-gated fail-closed error + port to Stage B |

**Bottom line for 0063 → packages:** it introduces **no new package** and **confirms the containment core is
already shipped** across `framework-app-kernel` + `core-compiler` + `governance/` + `tower-citizen`. The one
genuine package-touching build it points at is in **`@galerinaa/core-compiler`** (escalate `SPORE-EFFECT-002`;
extend the effects-only fingerprint to a full contract digest), plus the new audit-graph tooling (0064).

## 3. Gap-check — the 14 actionable items vs. the existing KB/roadmap

Verified by grepping every tracker (build-roadmap, task-ledger, fault-tolerance, framework-plan, formal-verification-direction,
pipeline-security-posture, certified-package-registry, contract-driven-generation, key-custody). **0059 is the only
number the docs reference (5 hits, all in the fault-tolerance doc); 0060–0064 appear nowhere by number** — the
dominant signal that the supply-chain / package-tier / type-K3 items are untracked.

### Already TRACKED (4) — no action beyond a pointer
- **(8) Global Safety Theorem SMT obligations** (n-ary K3 monotonicity + dispatch-totality → 0024/0040 Z3 track) — [galerina-fault-tolerance-and-stability.md](galerina-fault-tolerance-and-stability.md) §10-11 + [galerina-formal-verification-direction.md](galerina-formal-verification-direction.md).
- **(9) `SPORE-FAULT-005` (recover ⊨ invariant) + idempotency retry-legality** — fault-tolerance doc:259/280-290 + [galerina-resilience-observability-design.md](galerina-resilience-observability-design.md) (`SPORE-RES-001`). *(Idempotency is partially shipped: `resilience-inference.ts` hardcodes non-idempotent effects; the per-effect **annotation** is the net-new bit.)*
- **(11) Per-basic-block differential emit-validation** (extend 0014) — formal-verification-direction.md:38-40.
- **(3) Full contract digest beyond effects-only** — [galerina-pipeline-security-posture.md](galerina-pipeline-security-posture.md):49 ("#3 CFG fingerprint beyond effects-only", Phase-5 deferred).

### PARTIAL (3) — building blocks tracked, the specific delta is not
- **(1) Least-privilege minimality** — R&D 0016 generator tracked; `SPORE-EFFECT-002` detection shipped (warning). **Untracked delta:** escalate to a profile-gated fail-closed error + port to Stage-B. *(Convergent #1.)*
- **(6) Per-granted-capability egress-policy binding** — NetworkPolicy-level `EgressPolicy` shipped (build-roadmap:125-132); **untracked delta:** binding an egress policy to a *specific granted* `network.send`.
- **(10) Stage-A≡Stage-B per-build translation-validation certificate** — the 0014 harness is tracked as a differential test/proof; **untracked delta:** emitting it as a **signed per-build manifest artifact**.

### UNTRACKED (7) — net-new, not in any tracker
- **(2) Transitive capability-mask `⊆` proof down the dependency graph** (0062 §3 key novelty). Confirmed ABSENT: only intra-module flow-call-graph transitive-effect checks (`SPORE-EFFECT-002`/`collectTransitiveCalledEffects`) and the policy-hierarchy subset proof (`SPORE-INHERIT-001/002`) exist — neither is a cross-*package* mask proof. fuse-loader does per-module load-time deny-by-default only.
- **(4) Fixed package-standard profile + basic-rules checker** as an implicit level-1 dep (0062 §2/§4). ABSENT.
- **(5) `@galerinaa-core/*` verified-namespace tier + "deep deps may only depend on the verified tier"** (0062 §4). ABSENT (the [Certified Package Registry](certified-package-registry.md) has cert *levels*, not a namespace tier + depth rule).
- **(7) Signed-package audit graph** — `galerina graph --package <dir>` built-in + central auditor over the admission surface (0064). ABSENT (`galerina graph` covers monorepo source only; `galerina build --package` exists but not a graph/audit verb).
- **(12) Unify `TypeId.Unknown` parking with the governance K3 algebra** — one Kleene lattice across type-confidence + governance-verdict (0061 §2, **the headline compiler finding**). ABSENT.
- **(13) Key-custody hardening: name-similarity / expected-key check** (0063 §3 / 0057 residual #2). ABSENT (revocation registry is a different mechanism).
- **(14) Borrow TS7-native `tsc` as the host builder + 53-package build parallelism + watch** (0060). ABSENT (existing `tsc` refs are all #155 npm-workspace reproducibility).

## 4. Proposed task IDs (for the ledger)

New tasks, ranked by the convergent-value finding. Statuses: 🔲 pending · all design-only until built.

| # | Task | Source | Notes |
|---|---|---|---|
| **#201** | **Least-privilege minimality → fail-closed.** Escalate `SPORE-EFFECT-002 OVERDECLARED_EFFECT` from warning to a **production-profile error** (mirror the `SPORE-STDLIB-001` dev/prod toggle, `effect-checker.ts:258`); **port the check to the Stage-B `effect-checker.spore`** (currently omits 002). Non-breaking in dev. | 0062 §2 ∧ 0063 §3 (convergent #1) | highest value; low risk (detection exists) |
| **#202** | **Transitive capability-mask `⊆` proof** across the signed-package dependency graph (`effects(child) ⊆ mask(parent)`, compile/fuse-time). Generalises `SPORE-FAULT-002` from fault-handlers to the dep graph; extend `planComposition` / the resolver, not the fuse border. | 0062 §3 | the key novelty; closes deep-chain containment |
| **#203** | **Full contract digest** — extend the effects-only `behavioralFingerprint` to cover `limits{}`/`substrate{}`/`invariant{}` (tamper-bind the whole `contract{}`, not just effects), for the AI dual-role binding. | 0062 §5 / 0063 (#3 tracked, Phase-5 deferred) | aligns with pipeline-security-posture #3 |
| **#204** | **Signed-package audit graph** — `galerina graph --package <dir>` built-in + a central signed-package auditor over the admission surface (mask chain + provenance + 0063 attack surface). Reuse the `devtools-graph-project` engine; derived-from-verified only. | 0064 | makes #201/#202 inspectable before fusing |
| **#205** | **Unify `TypeId.Unknown` with the governance K3 algebra** — one Kleene lattice (`Known < Unknown < Conflicting` ∘ `DENY < INDETERMINATE < ALLOW`) so an unresolved type + an undischarged obligation compose monotonically into one fail-closed verdict. | 0061 §2 (headline) | larger; design-first |
| **#206** | **Package-standard profile + basic-rules checker** — a fixed profile (`effects{}` + `invariant{ensure result}` + `limits{}` + signed `.lmanifest` + provenance) every 3rd-party package must satisfy; ships as an implicit level-1 dep. + the `@galerinaa-core/*` verified-namespace tier + level-1-only deep-dep rule. | 0062 §2/§4 | depends on #201/#202 |
| **#207** | **Idempotency effect-annotation** enabling `resilience{}` retry-legality checking (turn the hardcoded non-idempotent list into a declared per-effect annotation). Unblocks the fallback-half of `SPORE-FAULT-005`. | 0059 §6 | small; fault-handler-runtime gated |
| **#208** | **Per-granted-capability egress-policy binding** (bind an `EgressPolicy` to a specific granted `network.send`). | 0063 §3 | refinement of shipped NetworkPolicy egress |
| **#209** | **Key-custody hardening** — optional name-similarity / expected-key check at the grant/sign boundary (the 0063 soft spot / 0057 residual #2). | 0063 §3 / 0057 | the human-control soft spot |
| **#210** | **TS7-native `tsc` host-build adoption** — gate on TS7 stability + project-references; + package-level (53-pkg) build parallelism + watch. Run the §3 baseline bench first. | 0060 | DX/CI; owner-go'd; NOT a compiler rewrite |
| — | **Signed per-build Stage-A≡Stage-B certificate artifact** (emit the 0014 differential result into the signed manifest). | 0059 §4 | partial; folds into the 0024 track |

**Not separate tasks (already moving / absorbed):** the 0059 Global Safety Theorem SMT obligations (#8) ride the
existing 0024/0040 Z3 track; `SPORE-FAULT-005` (#9) is documented + enforcement-gated in the fault-tolerance doc;
per-block differential (#11) is in formal-verification-direction.md. **Owner-gated steers** to surface before
building: #201 (breaking-in-prod), #205 (architecture), #210 (toolchain commitment).

## See also
`_session-bridge/done/0059..0064.done.md` (authority) · [galerina-fault-tolerance-and-stability.md](galerina-fault-tolerance-and-stability.md) (0059 home) ·
[galerina-build-roadmap.md](galerina-build-roadmap.md) · [galerina-task-ledger.md](galerina-task-ledger.md) ·
[galerina-rd-corpus-closure-2026-06-18.md](galerina-rd-corpus-closure-2026-06-18.md) (0036–0052) ·
[galerina-framework-plan-2026-06-21.md](galerina-framework-plan-2026-06-21.md) (supply-chain / fusion border).
