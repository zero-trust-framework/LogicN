# Work-with-it sweep — refused R&D reviewed for a govern-don't-absorb path (table)

**R&D block:** the "work with it" sweep (workflow `wb3pxv27p`, 50 agents, 24 refused items, each adversarially verified). Applies R&D Standard #21 (every refusal ships a work-with-it). **0 rationalizations smuggled through** — every CONFIRM was re-checked against the canonical refutation docs + shipped source; no hard line (crypto-on-noisy, FTL, drop-the-MAC, latency-as-work, bespoke hypervisor) was reintroduced.
**Bottom line:** the fence IS the feature — most-secure == most-marketable. Asymptotic/free-O(1)/FTL hype stays uniformly refuted; only the *governed* latency/depth + isolation + algebraic-collapse value is captured.

**ZT-score legend:** how zero-trust-sound our HANDLING is (capture-the-value-fail-closed, or refuse-to-absorb-the-unsafe). Higher = more secure.

## A. WINNERS — buildable now (CPU-side, honest govern-don't-absorb)

| # | Name | Explanation (the work-with-it) | Using it? | ZT |
|---|---|---|---|---|
| W1 | Photonic "O(1) matmul" | Admit the optical mesh as an untrusted Tier-3 **compute-only** lane (deny crypto/network/ledger); binary core does verify-before-decrypt, hands plaintext numbers out; **Freivalds** O(kN²) cheap-verify + signed **toleranceWitness**; result degrade-only (vAnd=min). O(1)-free-*work* stays refuted. | Buildable now (rails shipped; emulator) | 95 |
| W2 | Passive-transform-as-control-flow | Branch / AND / fail-closed collapse **stay binary** (decideAtBoundary); PartitionDecider routes only the **linear/tensor** sub-computation to the lane, only on a proven Safe-Floor net-win. | Buildable now (PartitionDecider shipped) | 94 |
| W3 | Tree-as-tensor | The K3 verdict fold is a **ternary-semiring reduction** (min/max/neg) → one associative reduction + an exact affine-flatten of the linear part; the matmul claim stays refuted. | Buildable now (CPU) | 90 |
| W4 | Photonic SHA-256 | Keep the digest **bit-exact digital** (ML-DSA-signed); photonics stays **outside** the hash (entropy source / content-addressing accelerator), re-verified. No crypto on a noisy lane. | Buildable now | 92 |
| W5 | "Drop ZT a bit" for popularity | **Invert**: never subtract enforcement — ADD an opt-in `@experimental_profile { compute_only }` deny-by-default fence. The fence is the GTM moat. | Buildable now | 96 |
| W6 | Optical PUF as sole auth | Demote to **one optional non-raising K3 auth factor** folded by `composeAuthVerdict = allOf = min` — it can ADD a refusal, never manufacture an ALLOW (No-Coercion). | Buildable now (galerina-auth) | 91 |
| W7 | 3D-spatial `tower-spatial-mesh` packages | Collapse the two HW-dispatcher packages into **one** `.lmanifest` Tier-3 compute-only capability artifact + emulator routing/compute models. | Buildable now (emulator) | 89 |
| W8 | WASM `memory.fill(0)` "O(1) wipe" | Drop the false "O(1) work" label; keep the three real properties: **atomic / race-free / vectorized** bulk wipe over `[base,heap)` (emitter already phrases it right). | **In progress** (worker's `overclaim-phrases` lint) | 90 |
| W9 | Transitive crypto-pin invariant | Value already captured by per-flow lanes + `effects⊇body` + the per-flow check → a **confirmatory verify-test**, build nothing. (Matches the A-1 refutation.) | Confirmatory test only | 93 |
| W10 | (Affine-flatten companion of W3) | Exact const-fold of the linear governance sub-tree on binary silicon — captured in W3. | Buildable now | 88 |

## B. TRACK-WHEN-CONSUMER — real governed path, gated on a consumer / hardware

| # | Name | Explanation | Using it? | ZT |
|---|---|---|---|---|
| T1 | AlphaDev sort network | Sort stays on binary silicon; the real value is a **data-oblivious / constant-time** sort macro — buildable, but **no consumer** (no array-sort builtin). | Track | 85 |
| T2 | Superdense coding (2× fiber) | The quantum 2×-per-photon stays refuted (Holevo); a coherent-channel form is **HW-gated**. | Track | 84 |
| T3 | Grover DB-search co-processor | As a DB engine stays hard-refuted (Ω(N) prep + Õ(N^{3/2})); only a niche governed oracle is conceivable, **HW-gated**. | Track | 82 |
| T4 | Governance DCE / const-fold | Pure CPU-side, but **owner-gated** (a build, not yet greenlit). | Track (owner) | 86 |
| T5 | Drop-the-MAC → tamper-evidence | "Drop the MAC" stays refuted (QBER ≠ EUF-CMA); QBER as an **additive** channel-disturbance *signal* alongside the MAC is governable, **HW-gated**. | Track | 83 |

## C. STAYS-REFUTED-HARD — physics/math forbids it; the work-with-it IS the existing posture

| # | Name | One-line hard reason | Using it? | ZT |
|---|---|---|---|---|
| R1 | `resolve … at boundary` construct | No device/noisy-output to govern (pure CPU parser surface); the safe form already ships as K3. | No | 92 |
| R2 | "-1 = recheck-later" construct | The unsafe reading is the `deliverAsync` fail-open; the safe form is shipped K3. | No | 92 |
| R3 | Bespoke Type-1 hypervisor / core-pinning | TCB growth, can't beat Spectre; delegate to WASM/Wasmtime. | No | 95 |
| R4 | Crypto-on-noisy substrate (photonic SHA-256 as the digest) | A hash needs zero-error strings; analog noise ⇒ broken integrity (FUNGI-SUBSTRATE-001). | No | 98 |
| R5 | QBER fed into the KDF as key material | Crosses FUNGI-SUBSTRATE-001 (keying stays binary). | No | 97 |
| R6 | MBQC as instantaneous / FTL logic | No-signaling is a theorem (`ρ_B=Tr_A(ρ_AB)` invariant). | No | 96 |
| R7 | 3D free-space beam-crossing as a logic gate | Photons are bosons — beams superpose linearly and pass through; need an MZI. | No | 94 |
| R8 | Ternary "exponentially fewer gates" | Radix-3 saves only the constant `log2(3)≈1.58`, not an asymptotic factor. | No | 93 |
| R9 | Holographic "O(1)-read petabyte sugar-cube" | ~10⁵× above demonstrated density; random access is a Bragg search, not O(1). | No | 95 |

*Full per-item mechanisms + the adversarial rulings are in the workflow transcript; the standing rule that generated this is R&D Standard #21.*
