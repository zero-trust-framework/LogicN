# 082 — ReadOnlyView

**Concept:** ReadOnlyView<T> generic type

ReadOnlyView<T> wraps a collection and exposes only read operations. It takes exactly one type parameter.

**AI rule:** ReadOnlyView<T> exposes a collection without mutation methods.
