# 053 — String basic

**Concept:** String type

String holds UTF-8 text. A string literal assigned to a typed binding is valid. For untrusted external input, use unsafe let instead.

**AI rule:** String holds UTF-8 text. Untrusted strings should be wrapped in unsafe let.
