# 069 — Auto type inference

**Concept:** Auto type inference

Auto instructs the compiler to infer the type from the initializer. Here 42 infers as Int. An initializer is required when using Auto.

**AI rule:** Auto means infer the type from the initializer expression.
