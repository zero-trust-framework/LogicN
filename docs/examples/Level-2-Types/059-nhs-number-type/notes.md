# 059 — NhsNumber type

**Concept:** protected NhsNumber domain type

NhsNumber is a protected domain type for NHS identifiers. Raw strings from external sources must be validated via alidate.nhsNumber(...).

**AI rule:** NhsNumber values come from alidate.nhsNumber().
