/// example: 067-result-error
/// level: 2
/// concept: Result<T,E> Err variant
/// expected_diagnostics: none
/// test_status: stable
/// ai_rule: Use Err(errorValue) to represent a typed failure in Result<T,E>.

pure flow example() -> Result<User, UserError> {
  let result: Result<User, UserError> = Err(UserError.NotFound)
  return result
}
