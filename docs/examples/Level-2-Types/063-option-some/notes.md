# 063 — Option Some

**Concept:** Option<T> with Some value

Option<T> is a generic type with two variants: Some(value) (value present) and None (absent). It takes exactly one type parameter.

**AI rule:** Option<T> represents a value that may be absent. Some(value) wraps a present value.
