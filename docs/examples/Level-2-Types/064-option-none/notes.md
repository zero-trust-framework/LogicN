# 064 — Option None

**Concept:** Option<T> with None value

None is the absent variant of Option<T>. The type annotation supplies the type parameter so the compiler can infer the expected type.

**AI rule:** None represents the absence of a value in Option<T>.
