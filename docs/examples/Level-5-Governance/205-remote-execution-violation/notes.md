# 205 — Remote execution violation

**Concept:** Compute target emote violates governance policy

When intent or policy restricts execution to on-premises, specifying compute target remote is a governance violation. The policy engine checks compute target against intent and governance rules.

**AI rule:** Specifying emote as the compute target when governance denies it is a policy violation.
