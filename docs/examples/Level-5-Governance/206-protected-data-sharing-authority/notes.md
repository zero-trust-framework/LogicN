# 206 — Protected data sharing authority

**Concept:** uthority block for sharing protected data externally

The uthority block declares explicit permission to share protected PatientData with a named external service. It must reference an approval (e.g., a governance board decision). Without the uthority block, sharing protected data externally is a policy error.

**AI rule:** Use an uthority block to declare permission to share protected data externally.
