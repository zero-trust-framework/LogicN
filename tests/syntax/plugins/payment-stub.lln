;; payment-stub.lln — Stub plugin for Phase 5 completion test
;; Simulates a payment gateway assimilated into the DSS hot pool.

pure flow stub_check(amount: Int) -> Bool
contract {
  intent "Stub payment check — always returns true"
  effects {}
}
{
  return true
}
