;; Library file — imported by import-test-main.lln

pure flow addTwo(x: Int) -> Int
contract {
  intent "Add 2 to a number"
  effects {}
}
{
  return x + 2
}

pure flow multiplyByThree(x: Int) -> Int
contract {
  intent "Multiply by 3"
  effects {}
}
{
  return x * 3
}
