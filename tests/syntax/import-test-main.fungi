;; Main file — imports from import-test-lib.fungi

import "./import-test-lib.fungi"

pure flow calculateResult(n: Int) -> Int
contract {
  intent "Calculate result using imported flows"
  effects {}
}
{
  let a = addTwo(n)
  let b = multiplyByThree(a)
  return b
}
