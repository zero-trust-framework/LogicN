;; Main file — imports from import-test-lib.lln

import "./import-test-lib.lln"

pure flow calculateResult(n: Int) -> Int
contract {
  intent "Calculate result using imported flows"
  effects {}
}
{
  let a = addTwo(n)
  let b = multiplyByThree(a)
  return b
}
